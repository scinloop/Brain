{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "start": "electron .",
    "packager": "electron-packager . Abc --platform=win32 --arch=x64 --out=./build --electron-version 25.3.1 --app-version 1.0.0 --overwrite --icon=head.ico"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^17.1.2"
  },
  "dependencies": {
    "vue": "^3.3.6"
  }
}
